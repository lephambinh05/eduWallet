{
  "name": "pzo-contract-deploy",
  "version": "1.0.0",
  "description": "Deploy smart contracts to Pione zerochain",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network pzo",
    "test": "npx hardhat test"
  },
  "keywords": ["blockchain", "smart-contract", "pzo", "hardhat"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
